# WNotes

一个简洁的 Windows 笔记应用。

## 项目概述

WNotes 是一个 Windows 平台桌面笔记应用，采用左右分栏布局，提供简洁高效的笔记管理功能。

### 产品定位
- 轻量级本地笔记应用
- 专注于核心的笔记编辑和管理功能
- 无复杂功能，简单易用

### 核心技术栈

**核心技术栈**：Electron + Vue3 + SQLite + 本地文件目录

**存储核心**：结构化文本（笔记信息）存在 SQLite，二进制图片存在本地目录，分离存储更高效。

**关键依赖**：
- **better-sqlite3**：SQLite 操作
- **Vue3**：前端框架
- **Electron**：桌面应用框架

---

## 界面设计

### 整体布局

```
┌─────────────────────────────────────────────────────────┐
│  WNotes                                    □ - ×       │
├─────────────────┬───────────────────────────────────────┤
│                 │                                       │
│   笔记列表      │           笔记编辑器                   │
│   (左侧面板)    │           (右侧面板)                  │
│                 │                                       │
│                 │                                       │
└─────────────────┴───────────────────────────────────────┘
```

### 左侧面板 - 笔记列表

**功能组件**:
- 新建笔记按钮（置顶）
- 笔记列表项（垂直排列）
- 删除笔记按钮（每个笔记项）

**列表项显示内容**:
- 笔记标题
- 最后更新时间

**交互行为**:
- 点击笔记项 → 选中该笔记，右侧显示内容
- 点击新建按钮 → 创建新笔记，自动选中
- 点击删除按钮 → 删除该笔记（需确认）

### 右侧面板 - 笔记编辑器

**功能组件**:
- 标题输入框（单行文本）
- 富文本编辑器（支持纯文本 + 图片）
- 图片插入（拖拽和粘贴）

**界面布局**:
```
┌─────────────────────────────────────┐
│ [标题输入框]                         │
├─────────────────────────────────────┤
│                                     │
│         笔记内容区域                 │
│      （支持文字和图片）              │
│                                     │
└─────────────────────────────────────┘
```

**交互行为**:
- 标题编辑 → 实时更新左侧列表
- 内容编辑 → 支持纯文本和图片
- 插入图片 → 支持拖拽和粘贴
- 自动保存 → 内容变化后自动保存
- 未选中笔记时 → 显示提示"请选择或创建笔记"

---

## 功能需求

### 笔记管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 创建笔记 | 点击新建按钮创建新笔记 | P0 |
| 删除笔记 | 删除指定笔记，需二次确认 | P0 |
| 选择笔记 | 点击列表项选中笔记 | P0 |
| 列表排序 | 按更新时间倒序排列 | P0 |

### 笔记编辑

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 编辑标题 | 单行文本输入 | P0 |
| 编辑内容 | 富文本格式（纯文本+图片） | P0 |
| 标题同步 | 标题变化时左侧列表实时更新 | P0 |
| 自动保存 | 内容变化后自动保存到本地 | P0 |
| 拖拽图片 | 拖拽图片到编辑区域显示 | P0 |
| 粘贴图片 | 从剪贴板粘贴图片 | P0 |
| 图片显示 | 编辑区域直接显示图片 | P0 |

### 数据持久化

| 功能 | 描述 | 优先级 |
|------|------|--------|
| SQLite 存储 | 使用 SQLite 存储笔记元数据 | P0 |
| 文件存储 | 使用 HTML 文件存储笔记内容 | P0 |
| 图片存储 | 使用本地目录存储图片 | P0 |
| 启动加载 | 应用启动时加载已有数据 | P0 |
| 自动保存 | 修改后自动保存，无需手动操作 | P0 |

---

## 数据结构

### SQLite 数据表

**表名**: `notes`

```sql
CREATE TABLE notes (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  file_path TEXT NOT NULL,
  updated_at INTEGER NOT NULL,
  created_at INTEGER NOT NULL
);
```

### 文件存储结构

```
data/
├── notes/                    # 笔记文件目录
│   ├── note-1.md
│   └── note-2.md
├── assets/
│   └── images/               # 图片资源目录
│       ├── note-1/
│       │   ├── image-1.jpg
│       │   └── image-2.jpg
│       └── note-2/
│           └── image-1.jpg
└── metadata.db               # SQLite 数据库文件
```

### 笔记文件格式

每个笔记保存为 HTML 文件（支持图片显示）：

```html
<h1>笔记标题</h1>

笔记内容...

<img src="D:\AppData\WNotes\data\assets\images\note-1\1234567890.png" alt="图片描述" />
```

---

## 项目结构

```
WNotes/
├── src/
│   ├── main/
│   │   ├── main.ts          # Electron 主进程入口
│   │   ├── preload.ts        # 预加载脚本
│   │   ├── database.ts      # SQLite 数据库操作
│   │   ├── file.ts          # 文件操作
│   │   ├── image.ts         # 图片处理
│   │   └── ipc-channels.ts  # IPC 通道定义
│   ├── renderer/
│   │   ├── App.vue          # Vue 根组件
│   │   ├── main.ts          # Vue 入口
│   │   ├── components/
│   │   │   ├── NoteList.vue     # 左侧列表组件
│   │   │   ├── NoteEditor.vue   # 右侧编辑器（纯文本+图片）
│   │   │   └── NoteItem.vue     # 列表项组件
│   │   ├── store/
│   │   │   └── index.ts         # Pinia 状态管理
│   │   └── utils/
│   │       └── debounce.ts      # 防抖工具函数
│   └── index.html
├── build/
│   └── icon.ico              # 应用图标
├── data/                     # 数据目录（运行时生成）
│   ├── notes/                # 笔记文件
│   ├── assets/
│   │   └── images/           # 图片资源
│   └── metadata.db           # SQLite 数据库
├── package.json
├── tsconfig.json
├── vite.config.ts
└── requirements.md
```

---

## 核心依赖

```json
{
  "dependencies": {
    "electron": "^40.0.0",
    "vue": "^3.4.0",
    "pinia": "^2.1.7",
    "better-sqlite3": "^12.6.0",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "electron-builder": "^24.9.0",
    "@types/better-sqlite3": "^7.6.8"
  }
}
```

---

## 开发计划

### 阶段一：项目搭建（1-2 天）
- [x] 初始化 Electron + Vue 3 + TypeScript 项目
- [x] 配置 Vite 构建工具
- [x] 安装核心依赖（better-sqlite3）
- [x] 创建基础项目结构

### 阶段二：核心功能（2-3 天）
- [x] 实现 SQLite 数据库操作
- [x] 实现文件系统读写
- [x] 实现左侧笔记列表组件
- [x] 实现右侧纯文本编辑器组件
- [x] 实现标题同步功能

### 阶段三：图片处理（1-2 天）
- [x] 实现图片拖拽上传
- [x] 实现图片粘贴上传
- [x] 实现图片显示

### 阶段四：完善优化（1-2 天）
- [x] 实现自动保存（防抖）
- [x] 实现删除确认对话框
- [x] 界面样式美化
- [x] 错误处理
- [x] 打包测试

---

## 验收标准

### 功能验收
- [x] 可以创建新笔记
- [x] 可以删除笔记（带确认）
- [x] 可以编辑标题和内容
- [x] 标题编辑时左侧列表实时更新
- [x] 内容修改后自动保存
- [x] 重启应用后数据不丢失
- [x] 支持拖拽图片到编辑区域
- [x] 支持粘贴图片
- [x] 图片显示正常

### 性能验收
- [x] 列表项点击响应无延迟
- [x] 标题输入无明显卡顿
- [x] 保存操作不影响用户体验

### 体验验收
- [x] 界面简洁美观
- [x] 交互流畅自然
- [x] 错误提示友好

---

## 不包含的功能

以下功能明确**不包含**在本版本中：

- ❌ 标签/分类功能
- ❌ 云同步
- ❌ 主题切换
- ❌ 导出功能
- ❌ 快捷键系统
- ❌ 笔记加密
- ❌ 版本历史
- ❌ 附件管理（除图片外）
- ❌ Markdown 语法支持
- ❌ Markdown 实时预览
- ❌ 富文本格式化（WYSIWYG）

---

## 功能特性

### ✅ 搜索功能

支持快速查找笔记的标题和内容：

- **搜索位置**：左侧笔记列表顶部
- **搜索范围**：笔记标题 + 笔记内容
- **实时搜索**：输入关键词自动显示匹配结果
- **清空搜索**：删除搜索关键词显示全部笔记

**使用方法**：
1. 在左侧面板顶部的搜索框中输入关键词
2. 系统自动过滤显示匹配的笔记
3. 点击任意笔记即可查看内容
4. 清空搜索框可显示全部笔记

---

## 数据备份与同步

### 📁 数据存储位置

笔记数据存储在应用安装目录下的 `data` 文件夹：

```
安装目录/
├── WNotes.exe
├── data/                     # 数据目录
│   ├── notes/                # 笔记文件
│   ├── assets/
│   │   └── images/           # 图片资源
│   └── metadata.db           # SQLite 数据库
```

### 💾 数据备份

**手动备份**：
1. 关闭 WNotes 应用
2. 复制整个 `data` 目录到安全位置
3. 需要恢复时，将备份的 `data` 目录复制回安装目录

**自动备份建议**：
- 使用文件同步工具（如 FreeFileSync、GoodSync）同步 `data` 目录
- 使用云盘同步（如 OneDrive、Dropbox）备份 `data` 目录
- 定期手动复制 `data` 目录到外部存储

### 🔄 数据同步

**多设备同步笔记**：
1. 在设备 A 上备份 `data` 目录
2. 将备份复制到设备 B 的安装目录
3. 在设备 B 上打开 WNotes，即可看到所有笔记

**注意事项**：
- 同步前确保应用已关闭
- 避免同时在多个设备上修改同一笔记
- 建议单向同步（从主设备到其他设备）

---

## 文档版本

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-03-09 | 初始版本 |
| v1.1 | 2026-03-09 | 增加 Markdown 和图片支持 |
| v2.0 | 2026-03-10 | 精简版：纯文本 + 图片编辑器，移除 Markdown |
| v2.1 | 2026-03-11 | 更新界面：标题 + 纯文本内容，支持拖拽/粘贴图片 |
| v2.2 | 2026-03-15 | 增加搜索功能，添加数据备份说明 |
